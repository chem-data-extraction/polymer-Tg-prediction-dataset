# Runtime dependencies for the Practice 5 build/clean/validate pipeline
pandas>=2.0
numpy>=1.24
 
# Testing
pytest>=7.4
 
# --- Optional, used in earlier practices (kept for reproducibility) ---
# Practice 3 (PDF extraction)
pdfplumber>=0.10
pymupdf>=1.23
# Practice 4 (web extraction)
requests>=2.31
beautifulsoup4>=4.12
# Optional structure handling (canonicalisation, polyimide assembly follow-up)
rdkit>=2023.9
